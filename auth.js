// auth.js
// NOTA: Este ficheiro foi substituído por auth-simple.js
// O sistema de autenticação agora usa auth-simple.js (sem Microsoft Entra ID / MSAL)
// Este ficheiro mantido apenas para compatibilidade de referência

if (typeof Auth === 'undefined') {
  console.warn('auth.js: use auth-simple.js instead of this file.');
}
